# PROJECT_MAP — MikroKiller

> Generated: 2026-05-27 | Role: Staff Engineer / Tech Lead

---

## [TECH_STACK]

| الطبقة | التقنية | الإصدار المستهدف |
|---|---|---|
| GUI Backend | Python 3.10+ | — |
| Web Bridge | Eel | 0.18.2 (⚠️ archived) |
| Native Window | pywebview | 6.2.1 |
| HTTP Client | requests | 2.34.2 |
| HTTP Low-level | urllib3 | 2.7.0 |
| Frontend | Vanilla HTML/CSS/JS | — |

### الاستثناءات المهملة (لن تستخدم)
- ❌ `customtkinter` (غير مستخدم في أي ملف)
- ❌ `beautifulsoup4` (غير مستورد في أي ملف)
- ❌ `Pillow` (غير مستخدم)
- ❌ `PatternAnalyzer` — domain معطل بـ `samples.txt` غير موجود
- ❌ `ProxyManager` — 15 سطر، لا يستحق ملفاً منفصلاً

---

## [ARCHITECTURE]

```
┌─────────────────────────────────────────────────────┐
│                    main.py                          │
│  Eel.init(web/) → Pywebview window → localhost:PORT │
└──────────┬──────────────────────────┬───────────────┘
           │ Eel RPC                   │ Eel RPC
           ▼                           ▼
┌──────────────────┐       ┌──────────────────────────┐
│   engine/        │       │     network/              │
│  ┌────────────┐  │       │  ┌──────────┐ ┌────────┐ │
│  │ Cracker    │──┼───────┼─▶│ Scanner  │ │ Spoof  │ │
│  │ (Threads)  │  │       │  │ (portal) │ │ (MAC)  │ │
│  │ Pause/Stop │  │       │  └──────────┘ └────────┘ │
│  └──────┬─────┘  │       └──────────────────────────┘
│         │        │
│  ┌──────▼──────┐ │       ┌──────────────────────────┐
│  │ Generator   │ │       │     notify/               │
│  │ (vouchers)  │ │       │  ┌──────────┐             │
│  └─────────────┘ │       │  │Messenger │             │
└──────────────────┘       │  │TG+Discord│             │
                           │  └──────────┘             │
                           └──────────────────────────┘

┌─────────────────────────────────────────────────────┐
│                    lib/ (مشتركة)                    │
│                   ┌──────────┐                      │
│                   │ logger.py│                      │
│                   └──────────┘                      │
└─────────────────────────────────────────────────────┘
```

### قواعد التبعية
- `engine/` ← `network/` (يحتاج adapter info)
- `engine/` ← `notify/` (يحتاج إرسال إشعار)
- `engine/` → `lib/logger`
- `network/` ← `lib/logger`
- `web/` ← `engine/` (عبر Eel RPC)
- `web/` ← `network/` (عبر Eel RPC)

---

## [SYSTEM_FLOW]

```
                     ┌──────────────┐
                     │  User opens  │
                     │  MikroKiller  │
                     └──────┬───────┘
                            │
                     ┌──────▼───────┐
                     │  Eel init +  │
                     │  Pywebview   │
                     └──────┬───────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
     ┌────────────┐ ┌────────────┐ ┌────────────┐
     │ Adapter    │ │ Settings   │ │ Console    │
     │ Tab       │ │ Tab        │ │ Tab        │
     └──────┬─────┘ └──────┬─────┘ └──────┬─────┘
            │              │              │
            ▼              ▼              │
     ┌────────────┐ ┌────────────┐       │
     │ Select     │ │ Set Target │       │
     │ adapter(s) │ │ URL, Auth  │       │
     └────────────┘ │ Mode, etc  │       │
                    └──────┬─────┘       │
                           │             │
                    ┌──────▼──────┐      │
                    │ Click Scan  │      │
                    │ (optional)  │      │
                    └──────┬──────┘      │
                           │             │
              ┌────────────▼────────┐    │
              │  Click START        │    │
              │  api_start_attack() │    │
              └────────────┬────────┘    │
                           │             │
              ┌────────────▼────────┐    │
              │  Cracker loop:      │    │
              │  generate batch →   │◄───┘
              │  submit to pool     │
              │  → check response   │
              └────────────┬────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
         ┌────────┐ ┌──────────┐ ┌──────────┐
         │ 302 /  │ │ 403 / 429│ │ Exception│
         │ Success│ │ BAN      │ │ (timeout)│
         └───┬────┘ └────┬─────┘ └────┬─────┘
             │           │            │
             ▼           ▼            │
         ┌────────┐ ┌──────────┐      │
         │ Save + │ │ Auto MAC │      │
         │ Notify │ │ Spoof    │      │
         └────────┘ └──────────┘      │
             │           │            │
             ▼           ▼            │
         ┌────────┐ ┌──────────┐      │
         │ Valid  │ │ Wait re- │      │
         │ found  │ │ connect  │      │
         └────────┘ └──────────┘      │
                                       │
             ┌─────────────────────────┘
             ▼
       ┌────────────┐
       │ Stop or    │
       │ continue   │
       └────────────┘
```

---

## [ORPHANS & PENDING]

| المعرف | المشكلة | الأولوية |
|---|---|---|---|
| ~~P1~~ | ~~`requirements.txt` غير دقيق — يحتوي 3 حزم وهمية، وينقص `eel` + `pywebview`~~ | ~~**HIGH**~~ **FIXED** |
| P2 | Eel repository مؤرشف — خطر مستقبلي. استبداله يتطلب إعادة بناء كاملة (خارج نطاق الجراحة) | MEDIUM |
| ~~P3~~ | ~~`SourceAddressAdapter.send()` يستخدم monkey-patch لـ `socket.create_connection` — غير Thread-safe~~ | ~~**HIGH**~~ **FIXED** |
| ~~P4~~ | ~~`executor.shutdown(wait=False)` يقتل طلبات أثناء تغيير thread pool~~ | ~~MEDIUM~~ **FIXED** |
| P5 | `local_config.json` يحتوي Telegram token حقيقي + chat ID (متعمد — مشروع خاص) | INFO |
| ~~P6~~ | ~~PatternAnalyzer عاطل~~ | ~~LOW~~ **FIXED** |
| ~~P7~~ | ~~ProxyManager — 15 سطر في ملف منفصل~~ | ~~LOW~~ **FIXED** |
| ~~P8~~ | ~~`license.dat` والنظام الكامل للترخيص (hwid.py + SECRET_SALT)~~ | ~~LOW~~ **REMOVED 22 May** |
| ~~P9~~ | ~~`stealth: true` + `threads: 100` عدوانية جداً~~ | ~~MEDIUM~~ **FIXED** |
| ~~P10~~ | ~~`cracker_errors.log` في CWD بدون مجلد~~ | ~~LOW~~ **FIXED** |
| ~~P11~~ | ~~Phantom bound IPs تتراكم~~ | ~~**FIXED**~~ |
| ~~C1~~ | ~~`cracker_config.json` ميت — لم يعد له مرجع~~ | ~~LOW~~ **DELETED 27 May** |
| ~~C2~~ | ~~`Session_4_invalid.txt` و `Session_4_valid.txt` في جذر المشروع~~ | ~~LOW~~ **MOVED to sessions/ 27 May** |
| ~~C3~~ | ~~`logs/strip_comments.py` — سكريبت مؤقت~~ | ~~LOW~~ **DELETED 27 May** |
| ~~C4~~ | ~~`cracker_errors.log` في جذر المشروع~~ | ~~LOW~~ **DELETED 27 May** |
| ~~C5~~ | ~~Logger يستخدم tkinter (blocking)~~ | ~~LOW~~ **FIXED 27 May** |
| C6 | `ProxyManager` inline في cracker.py (15 سطر) — مقبول كما هو | INFO |
| ~~C7~~ | ~~Config يحتوي `discord_token` + `discord_channel` ولكن غير معروض في GUI~~ | ~~MEDIUM~~ **FIXED 27 May** |
| C8 | `Session_4_invalid.txt` الأصلي لا يزال في الجذر (مقفل بواسطة عملية أخرى — تم النسخ إلى sessions/) | LOW |
| ~~C9~~ | ~~lab_server.py خطأ UnicodeEncodeException مع أحرف box-drawing~~ | ~~LOW~~ **FIXED 27 May** |
| ~~S1~~ | ~~MAC spoofing بطيء: Disable/Enable + sleep(1.5) → Restart-NetAdapter~~ | ~~HIGH~~ **FIXED 27 May** |
| ~~S2~~ | ~~`get_network_adapters()` يستخدم PowerShell (مهلة 10s) → wmic (559ms)~~ | ~~HIGH~~ **FIXED 27 May** |
| ~~S3~~ | ~~سجلات الكونسول كثيرة جداً: كل 50 محاولة، رسائل adapter verbose~~ | ~~MEDIUM~~ **FIXED 27 May** |
